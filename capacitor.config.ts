import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'codeswag-map',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
